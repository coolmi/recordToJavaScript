// 数组方法操作：
Array.join()  // 用于把数组中的所有元素放入一个字符串。
Array.reverse() // 翻转
Array.sort() // 排序
Array.concat() // 创建并返回新的数组，在原数组后面
Array.slice() // 返回指定数组的子数组 有两个参数（数组下表）（1,3）不包括后面的参数元素
Array.splice() // 插入或删除数组，返回删除数组 有两个参数（一个是下标，一个是删除个数）
Array.push() // 在原数组末尾添加一个或多个元素，并返回新的长度
Array.pop() // 用于删除并返回数组的最后一个元素。
Array.unshift() // 可向数组的开头添加一个或更多元素，并返回新的长度。
Array.shift() // 用于把数组的第一个元素从其中删除，并返回第一个元素的值。


